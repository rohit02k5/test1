#include<stdio.h>
int main()
{ 
int base,height;
float radius;
`int  length,breadth;
int choice;
scanf(" %d", &choice);
 switch(choice) {
case 1: {
printf("value of base and height");
scanf("%d %d", &base , &height);
printf("area %d", (base*height)/2; }
case 2: {
scanf("%f", &radius);
printf("the perimeter is %d", 3.14*r*2);
 printf("the area os %d ", 3.14*r*r);
}
case 3: { scanf("%d  %d", &length, &breadth);
printf("%d", (length*breadth));
printf("%d", 2*(length*breadth));
}
}
return 0;
}
